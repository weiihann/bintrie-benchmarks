diff --git a/main.go b/main.go
index 69d1e0b..d832471 100644
--- a/main.go
+++ b/main.go
@@ -18,6 +18,7 @@ import (
 	"fmt"
 	"log"
 	"math/big"
+	"net/http/httptest"
 	"os"
 	"os/signal"
 	"path/filepath"
@@ -28,6 +29,8 @@ import (
 	"time"
 
 	"github.com/ethereum/go-ethereum/common"
+	gethmetrics "github.com/ethereum/go-ethereum/metrics"
+	gethprom "github.com/ethereum/go-ethereum/metrics/prometheus"
 
 	"github.com/nerolation/state-actor/client/besu"
 	"github.com/nerolation/state-actor/client/geth"
@@ -76,6 +79,11 @@ var (
 	// Client selection (multi-client support). Each client uses its own
 	// self-contained machinery inside client/<name>/; only the CLI is shared.
 	client = flag.String("client", "geth", "Target Ethereum client: 'geth' (default), 'nethermind', 'besu', or 'reth'. Other clients (erigon) are planned in follow-up PRs.")
+
+	// Build-phase metrics: when --metrics-dump is set, enable geth's go-metrics
+	// (pebble disk/compaction/cache, pathdb commit, process disk IO) during the
+	// build and write the full prometheus dump to the given file at completion.
+	metricsDump = flag.String("metrics-dump", "", "Write geth prometheus metrics to this file at end of build (enables metrics collection).")
 )
 
 func main() {
@@ -95,6 +103,14 @@ func main() {
 		os.Exit(1)
 	}
 
+	// Enable geth go-metrics collection for the build if a dump was requested.
+	// Meters/counters always record their cumulative counts; Enable() also starts
+	// the rate ticker, and CollectProcessMetrics adds process-level disk IO.
+	if *metricsDump != "" {
+		gethmetrics.Enable()
+		go gethmetrics.CollectProcessMetrics(3 * time.Second)
+	}
+
 	if *seed == 0 {
 		*seed = time.Now().UnixNano()
 	}
@@ -402,6 +418,18 @@ func main() {
 		}
 	}
 
+	// Write the build-phase geth metrics dump (prometheus text) if requested.
+	if *metricsDump != "" {
+		rec := httptest.NewRecorder()
+		req := httptest.NewRequest("GET", "/debug/metrics/prometheus", nil)
+		gethprom.Handler(gethmetrics.DefaultRegistry).ServeHTTP(rec, req)
+		if err := os.WriteFile(*metricsDump, rec.Body.Bytes(), 0o644); err != nil {
+			log.Printf("WARN: failed to write metrics dump to %s: %v", *metricsDump, err)
+		} else {
+			log.Printf("Wrote build metrics to %s (%d bytes)", *metricsDump, rec.Body.Len())
+		}
+	}
+
 	// Keep stats server running after completion if enabled
 	if statsServer != nil {
 		fmt.Printf("\n=== Stats server still running at http://localhost:%d ===\n", *statsPort)
